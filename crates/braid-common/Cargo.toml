[package]
name = "braid-common"
version = "0.1.0"
edition = "2021"
description = "Shared utilities and directory management for Braid"

[dependencies]
tracing = "0.1"
anyhow = "1.0"
serde = { version = "1.0", features = ["derive"] }
uuid = { version = "1.0", features = ["v4"] }
